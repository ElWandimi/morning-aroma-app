{
  "name": "morning-aroma",
  "private": true,
  "version": "0.1.0",
  "description": "Morning Aroma — an interactive frontend prototype for a specialty coffee brand, including a full storefront, customer dashboard, and admin panel.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-sitemap.mjs && vite build && node scripts/generate-route-meta.mjs",
    "start": "node server.cjs",
    "preview": "vite preview",
    "test:e2e": "playwright test"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions",
    "last 2 Safari versions",
    "last 2 Edge versions",
    "not dead",
    "not IE 11"
  ],
  "dependencies": {
    "express": "^5.2.1",
    "jspdf": "^4.2.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-react": "^4.4.0",
    "vite": "^6.4.3"
  }
}
